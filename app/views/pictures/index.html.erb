<h1>Pictures</h1>

<% link_to "New_Picture", "/Pictures/New" %>

<% @pictures.each do |picture| %>
	<p> <%=picture[:title] %>, <%=picture[:artist] %></p>
	<%= link_to image_tag(picture_url), "/picture/#{picture.id}" %>
<% end %>

