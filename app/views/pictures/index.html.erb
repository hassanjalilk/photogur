


<h1>Most recent pictures</h1>
<ul>
  <% @most_recent_pictures.each do |picture| %>
    <li>
    	<%= picture.title %><br>
    	<%= link_to image_tag(picture.url), picture %>
    </li>
  <% end %>
</ul>
